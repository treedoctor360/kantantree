# CLAUDE.md — かんたん樹木登録アプリ（仮称 kantantree）

最終更新: 2026-07-27
担当: Koh Kitsukawa（大津市公園緑地課 / 樹木医）

---

## 0. 目的とスコープ

**作業中に「この木、番号を振っておきたい」と思った瞬間に、3タップで1本を登録できる道具。**

点検・診断は行わない。健全度も活力度も撮らない。位置・樹種・番号・写真だけを残す。
「点検を行うほどではないが、樹木の所在は記録しておきたい」公園が対象。

### 既存システムとの関係（重要）

| システム | 役割 | 本アプリとの関係 |
|---|---|---|
| 樹木診断システム v27（`shindanv27`） | 個体単位の精密な診断カルテ | **座標入力UIを流用する**（後述3章）。将来の連携先 |
| 樹木台帳メモ（`parktreenote`） | 現場メモ・数量集計（樹種＋規格＋本数） | **公園登録の考え方を流用する**（後述4章） |
| サブ樹木医システム / 写真帳システム | — | 本アプリは干渉しない |

**やらないこと（明示的にスコープ外）**
- 健全度・活力度の入力
- 診断所見、対策提案
- オフライン動作（Service Worker / PWAキャッシュ）
- クラウド同期（GAS / Supabase）※将来検討、初版では実装しない

---

## 1. 前提条件（確定事項）

| 項目 | 決定 |
|---|---|
| 利用者 | 当面 Koh のみ。他職員が使う可能性あり（初版では考慮不要、データ形式だけ将来に備える） |
| 保存先 | 端末内（IndexedDB）＋ JSONバックアップの書き出し／読み込み |
| オフライン | 不要 |
| 樹木番号 | 新規に自動採番（既存台帳の体系には合わせない） |
| 台帳連携 | 初版は「案A（ファイル受け渡し）」。ただしデータ構造は「案C（共通スキーマ）」を満たす形で作る（10章） |
| 写真 | 必要（1本あたり最大3枚） |
| 転職後の利用 | 未定。**大津市固有の値をコードに直書きしない**（公園名・樹種はすべてユーザーが登録するデータとして扱う） |

---

## 2. 技術スタック

v27 と同一構成にする（部品を流用するため）。ただし不要なものは削る。

| 層 | 採用 | 備考 |
|---|---|---|
| ビルド | Vite | v27 と同じ |
| UI | React | v27 と同じ |
| ローカルDB | Dexie.js | IndexedDB（ブラウザ内DB）の薄いラッパー |
| 状態管理 | **なし**（Dexie の `useLiveQuery` ＋ React の `useState`） | この規模では Zustand は過剰。v27 から削る |
| 地図 | React-Leaflet ＋ 国土地理院タイル | v27 と同じ |
| PWA | **なし**（`manifest.json` のみ置く） | オフライン不要のため Service Worker は入れない。ホーム画面追加のアイコンだけ欲しいので manifest は置く |
| デプロイ | GitHub Pages | `vite.config.js` の `base: '/kantantree/'` |

> ライブラリのバージョンは着手時に最新の推奨を確認して確定すること。

---

## 3. 座標入力UI（v27 から流用・仕様固定）

**v27 の「カルテ」タブ内の座標ブロックと同一の見た目・挙動にする。**
参照元: `shindanv27` の `src/components/.../LocationPicker.jsx` 周辺。
実装前に必ず v27 の実ソースを読み、UI文言・関数名を合わせること。

### 3-1. 画面要素（上から順）

```
座標（近接木の距離判定に使用）     ← 見出し（本アプリでは「座標」だけでよい）
  緯度 Latitude   [ 35.01776443639602 ]   ← テキスト入力。フル桁を保持する
  経度 Longitude  [ 135.85462927807734 ]
  [📍 現在地を入れる] [クリア]  地図で確認   ← 3ボタン横並び（3つ目はリンク体）
  35.017764, 135.854629（現在地）           ← 現在地の参考表示（丸め6桁）
  [Googleマップのリンク / 35.0…] [読み取り]  ← 貼り付け欄＋読み取りボタン
  現在地を取得しました（誤差 約9m）。         ← ステータス行
  後日の登録でも、現地のGoogleマップのリンクや
  緯度経度を貼り付ければ座標を残せます。      ← 補足文（そのまま流用）
```

### 3-2. 挙動

- **📍現在地を入れる**
  `navigator.geolocation.getCurrentPosition` を
  `{ enableHighAccuracy: true, timeout: 10000, maximumAge: 0 }` で呼ぶ。
  成功したら緯度・経度欄を埋め、`accuracy`（m）を保持して
  「現在地を取得しました（誤差 約◯m）。」と表示する。
  失敗時はエラー内容に応じた日本語メッセージを出す（許可拒否／タイムアウト／取得不可）。
- **クリア** — 緯度・経度・accuracy・coordSource を空にする。
- **地図で確認** — 地図モーダルを開き、ピンをドラッグまたはタップで補正できる。
  補正したら `coordSource` を `'map'` にする。
  樹冠下では衛星電波の反射（マルチパス）でGPS精度が落ちるため、**手で直せる導線は必須**。
- **読み取り（Googleマップリンク）** — 貼り付けられた文字列から緯度経度を抽出する。
  対応させる形式は最低限これら:
  - `https://maps.google.com/?q=35.0177,135.8546`
  - `.../@35.0177,135.8546,18z`
  - `...!3d35.0177!4d135.8546`
  - `35.0177, 135.8546`（座標のみのベタ貼り）
  抽出できたら `coordSource` を `'link'` にする。できなければ「座標を読み取れませんでした」と表示。
- **手入力** — 緯度経度欄を直接編集した場合は `coordSource` を `'manual'` にする。

### 3-3. 保持する値

```js
{ lat: Number|null, lng: Number|null, accuracy: Number|null,
  coordSource: 'gps' | 'map' | 'manual' | 'link' | null }
```

座標は**必須にしない**。後日まとめて入れられることを補足文で明示しているため、
座標なしでも登録できる（一覧で「座標なし」バッジを出す）。

---

## 4. 公園登録機能（parktreenote から流用）

参照元: `parktreenote` の `index.html`。実装前に実ソースの公園まわりを読むこと。

### 4-1. 機能

- 公園の**追加・編集・削除**（設定タブ）
- **公園コード**（例 `P001`）— 樹木番号の接頭辞に使う。**登録時に自動付与、編集可**（詳細は4-2）
- **公園名**（例 `皇子が丘公園`）
- **代表座標** — 「📍現在地を入れる」で登録。地図タブの初期表示位置に使う
- **公園タブによる絞り込み** — 一覧タブの上部に公園タブを横スクロールで並べる
- **この公園をまるごと削除** — 配下の樹木・写真も削除。件数付きの確認ダイアログを出し、
  削除は単一の IndexedDB トランザクションで実行する（parktreenote v3.1 と同じ方式）
- **並び順** — 最終使用日時の降順（よく使う公園が左に来る）

### 4-2. 公園コードと樹木番号の採番

#### 公園コード

- 公園を登録した瞬間に **`P001`, `P002`, …** を自動付与する。
  登録時にコードを考えさせない（入力欄は初期値入りで表示する）。
- 採番は「既存の公園コードのうち `P` ＋数字 形式のものの最大値 ＋1」を毎回計算する。
  カウンタは保存しない。欠番は埋めない。
- **編集可**。現地の樹名札に手書きする運用が出てきた場合に、短い記号
  （`KOJI` など）へ変更できるようにする。
- 制約は次の2つだけ。
  - **半角英数とハイフンのみ**（CSVやファイル名で壊れないため）
  - **重複不可**（保存前にチェックし、重複していたらエラーで止める）
- コード変更時は、**その公園に紐づく既存樹木の `treeNo` と `parkCode` を一括で書き換えるか**を
  確認ダイアログで尋ねる。「書き換える／このままにする」を選べるようにし、
  書き換えは単一の IndexedDB トランザクションで実行する。
- 公園コードが読みやすい必要はない。一覧・地図・CSVにはすべて**公園名**を表示する。

#### 樹木番号

書式: `{公園コード}-{連番3桁}` 例 `P001-004`

- 連番は「その公園に既存する樹木の連番の最大値 ＋1」を**毎回計算する**。
  カウンタを保存しない（削除時のズレを防ぐため）。**欠番は埋めない。**
- 登録画面には次の番号をプレビュー表示する。**編集可能**にする（手で上書きできる）。
- 同一公園内で番号が重複したら保存前に警告する（保存自体は許可し、一覧で重複バッジを出す）。

---

## 5. データ層設計（Dexie）

```js
db.version(1).stores({
  parks:    'id, code, name, lastUsedAt',
  trees:    'id, parkId, treeNo, species, updatedAt, [parkId+treeNo]',
  photos:   'id, treeId',        // 写真は別テーブル（一覧の描画を軽くするため）
  settings: 'key'                // 樹種の使用履歴など単一値
});
```

### park

```js
{
  id,            // UUID
  code,          // 公園コード 'P001'（自動付与・編集可・重複不可）
  name,          // '皇子が丘公園'
  lat, lng,      // 代表座標（任意）
  note,
  lastUsedAt, createdAt, updatedAt
}
```

### tree

```js
{
  id,                    // UUID（共通スキーマの主キー。10章 案C）
  parkId,
  parkCode,              // 書き出し時に公園コードが単体で追えるよう非正規化して持つ
  treeNo,                // 'P001-004'
  species,               // 樹種和名。'不明' も可
  lat, lng, accuracy, coordSource,
  height,                // 樹高(m) 任意
  girth,                 // 幹周(cm) 任意
  note,                  // メモ 任意
  registeredAt,          // 登録日時
  updatedAt              // 更新のたびに更新（マージ時の競合解決に使う）
}
```

### photo

```js
{ id, treeId, dataUrl, order, takenAt }
```

- 保存前にリサイズする: **長辺 1280px / JPEG 品質 0.7**。
- 1本あたり最大3枚。
- IndexedDB の容量は無限ではない。設定タブに**概算使用量**（`navigator.storage.estimate()`）を表示する。

---

## 6. 画面構成

タブは4つ。**起動時は「登録」タブ**を開く。

### 6-1. 登録タブ（最重要・ここの摩擦をゼロにする）

上から順に:

1. **公園セレクタ** — ボタン式（最近使った公園が左）。未登録なら「公園を登録してください」導線
2. **次の番号プレビュー** — `P001-004`（タップで編集）
3. **樹種タイル** — よく使う樹種を12枚のタイルで表示（使用回数の降順）。
   末尾に「その他（入力）」と「不明」を常設。**前回選択した樹種を初期選択にする**
4. **座標ブロック**（3章の仕様そのまま）
5. **写真** — 「📷 撮影」ボタン（`<input type="file" accept="image/*" capture="environment">`）。
   サムネイル横並び、タップで削除
6. **任意項目（折りたたみ）** — 樹高 / 幹周 / メモ
7. **固定フッターの大きな［登録］ボタン**

**登録直後の挙動（連続登録のため）**
- トースト表示「P001-004 を登録しました」
- 公園・樹種は**保持**する
- 座標・写真・任意項目は**クリア**する
- 番号プレビューを +1 する

目標: 公園と樹種が同じなら「📍現在地 → 📷撮影 → 登録」の3タップ。

### 6-2. 一覧タブ

- 上部に公園タブ（横スクロール）
- カード表示: 樹木番号 / 樹種 / サムネイル / 座標の有無 / 登録日
- 検索（樹木番号・樹種・メモの部分一致）
- タップで編集画面（登録タブと同じフォーム）
- **選択して一括削除**モード（parktreenote v3.1 と同じ方式）

### 6-3. 地図タブ

- 国土地理院タイル。選択中の公園の代表座標を初期表示に使う
- 樹木ピン（樹種ごとに色分け、または一律で番号ラベル）
- ピンをタップ → 樹木番号・樹種・サムネイルのポップアップ →「編集」
- 座標なしの樹木は地図に出ない。件数だけ「座標なし 3件」と隅に表示する

### 6-4. 設定タブ

- 公園の管理（4章）
- 樹種マスタの管理（よく使う樹種の追加・削除・並び替え）
- **バックアップ** — JSON書き出し／読み込み（7章）
- **書き出し** — CSV / GeoJSON
- ストレージ使用量の概算表示
- 「最終バックアップから◯日」の表示（v27 と同じ考え方）

---

## 7. バックアップ・書き出し

| 形式 | 内容 | 用途 |
|---|---|---|
| **JSON（フルバックアップ）** | parks + trees + photos（Base64込み） | 端末移行・復元 |
| **JSON（軽量）** | parks + trees（写真なし） | 他アプリへの受け渡し |
| **CSV** | trees（写真なし・UTF-8 BOM付き） | Excel・台帳への転記 |
| **GeoJSON** | 座標のある trees | GIS・地図ソフトへの取込 |

**JSON読み込みは「追記マージ」を既定にする**（全上書きではない）。
- `id` 一致 → `updatedAt` が新しい方を採用
- 未知の `id` → 追加
- 実行前に「追加◯件 / 更新◯件」を確認ダイアログで提示する

CSVは UTF-8 BOM 付きで出力する（Excelの文字化け対策）。

---

## 8. フォルダ構成（案）

```
kantantree/
├─ index.html
├─ vite.config.js          # base: '/kantantree/'
├─ CLAUDE.md
├─ public/
│   └─ manifest.json       # ホーム画面追加用（SWは入れない）
└─ src/
   ├─ main.jsx
   ├─ App.jsx
   ├─ db/db.js                    # Dexie定義
   ├─ lib/
   │   ├─ geo.js                  # Googleマップリンクの座標抽出・現在地取得
   │   ├─ treeNo.js               # 自動採番（純粋関数）
   │   ├─ image.js                # 写真リサイズ
   │   └─ io.js                   # JSON/CSV/GeoJSON の書き出し・取込
   ├─ components/
   │   ├─ RegisterForm/           # 登録タブ
   │   ├─ LocationPicker/         # ★v27から流用
   │   ├─ SpeciesTiles/           # 樹種タイル
   │   ├─ PhotoInput/
   │   ├─ TreeList/               # 一覧タブ
   │   ├─ MapView/                # 地図タブ
   │   └─ Settings/               # 設定タブ（公園管理を含む）
   └─ styles/
```

---

## 9. 実装順

1. Vite + React の骨組み（`base` 設定を先に入れる）
2. **`src/db/db.js`（Dexie）と `src/lib/treeNo.js`（採番の純粋関数）** を先に作る
3. 公園管理（設定タブ）— これがないと何も登録できない
4. `LocationPicker` を v27 から移植して単体で動かす
5. 登録タブ（樹種タイル → 座標 → 写真 → 登録）
6. 一覧タブ
7. 地図タブ
8. バックアップ・書き出し
9. GitHub Pages デプロイ

採番ロジックにはテストを書く（`node --test`）。欠番あり・削除後・重複時の3ケースを最低限確認する。

---

## 10. 台帳連携（方針・初版では未実装）

### 採用方針

**案A（treeNo を横串キーにしたファイル受け渡し）で運用を始める。
ただしデータ構造は案C（共通スキーマ）を満たしておく。**

案Bは他職員が使い始めてから検討する。

### 案C 共通スキーマ（tree-core）— 本アプリが必ず満たす最小項目

```js
{ id, parkCode, treeNo, species, lat, lng, updatedAt }
```

- `id` は UUID。アプリをまたいでも衝突しない
- マージ規則は `id` 一致で `updatedAt` の新しい方を採用（v27 と同じ規則）
- 将来 GAS でも Supabase でも中継先を差し替えられる

### 未確認事項（実装前に Koh が判断すること）

【推定・要確認】parktreenote（樹木台帳メモ）は CHANGELOG を読む限り
「樹種＋規格＋本数」の**集計単位**のメモであり、個体単位の記録ではないように見える。
だとすれば個体レベルの突合はそもそも噛み合わない。
その場合、実際に価値のある連携は **公園マスタ（公園コード・公園名・代表座標）の共有** の方になる。
`parktreenote/index.html` の公園まわりを読んで、認識が正しいか確認すること。

### 各案の要約

| 案 | 内容 | 長所 | 短所 |
|---|---|---|---|
| A | treeNo を共通キーにし、CSV/JSONで受け渡し | 実装コスト最小・疎結合・壊れにくい | 手作業。リアルタイム性なし |
| B | v27 の GAS スプレッドシートに「樹木マスタ」シートを追加し双方向同期 | 一元管理。他職員と共有できる | v27側の改修とGAS設定が必要。ネット依存 |
| C | 3アプリ共通の最小スキーマを定義して相互取込 | 中継先を後から差し替えられる | 3アプリ全部に取込機能の実装が必要 |

---

## 11. コーディング方針（全プロジェクト共通）

- UIは日本語、コード内コメントも日本語でよい
- 後から自分で読めるコードを優先。過度な抽象化は避ける
- **差分修正の意味は「無関係な箇所を書き換えない」こと。**
  1件の依頼に伴う複数箇所の連動修正はまとめて実施してよい。
  事前合意が必要なのは「設計変更」「ファイル構成変更」「大幅な書き換え」のみ
- **修正報告は簡潔に。** 構文チェック・テストの結果は問題なければ一言で報告する
  （例:「npm test OK」）。問題があった場合のみ原因・修正内容を詳しく説明する
- プログラミング用語は初出時に日本語の補足を入れる
- ターミナル操作は省略せず毎回手順を明示する
- 事実と推定を分ける。推定には【推定】を付ける
- フッターに著作権表記を入れる

---

## 12. 開発の始め方

```bash
# 1. プロジェクト作成（React + JavaScript を選択）
npm create vite@latest kantantree
cd kantantree

# 2. 依存を入れる
npm install
npm install dexie dexie-react-hooks leaflet react-leaflet

# 3. 開発サーバ起動
npm run dev
```

その後この CLAUDE.md をリポジトリ直下に置き、`src/db/db.js` と `src/lib/treeNo.js` から着手する。

---

© 2026 Koh Kitsukawa. All rights reserved.
